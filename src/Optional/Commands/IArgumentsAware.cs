namespace Optional.Commands
{
    /// <summary>
    /// A marker interface which states the intent of an <see cref="ICommand"/>
    /// to process the supplied command-line arguments on its own.
    /// </summary>
    public interface IArgumentsAware
    {
    }
}